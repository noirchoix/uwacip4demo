<script lang="ts">import StateCard from '$lib/components/StateCard.svelte';import Panel from '$lib/components/Panel.svelte';let{data}=$props();</script>
<div class="page"><header class="page-head"><div><h1>What is true now?</h1><p>Resolve current reality without hiding uncertainty. Search the same engine across availability, access, working condition, time and material change.</p></div><a class="button" href="/report">Report what you see</a></header><div class="grid"><section>{#if data.current}<StateCard state={data.current}/>{:else}<div class="surface"><p class="error">{data.error}</p></div>{/if}</section><aside class="surface" style="position:static;height:auto"><h2 style="margin-top:0">Check a state</h2><form method="GET"><label>Entity ID<input name="entity" value={data.entity}/></label><label style="margin-top:12px">State type<select name="state_type" value={data.stateType}><option>AVAILABLE</option><option>ACCESSIBLE</option><option>WORKING</option><option>TIME</option><option>CHANGED</option></select></label><div class="button-row"><button>Check current state</button></div></form></aside></div><div style="margin-top:22px"><Panel title="Decision workflow"><ol><li>Check existing current state and freshness.</li><li>If inadequate, create or join one reality gap.</li><li>Acquire from the lowest-cost sufficiently reliable source.</li><li>Preserve evidence and contradictions.</li><li>Publish only when policy permits; otherwise keep uncertainty explicit.</li><li>Reuse the resulting state until it decays.</li></ol></Panel><Panel title="Developer boundary" open={false}><p class="technical">Observation ≠ canonical state · Declaration ≠ verification · Inference ≠ observation · Confidence ≠ authorization · UNKNOWN is valid.</p></Panel></div></div>
